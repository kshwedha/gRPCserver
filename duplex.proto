syntax = "proto3";

option go_package = "github.com/kshwedha/gRPCserver/otel";

service Otel {
    rpc Create(Request) returns (Response);
}

message Request {
    string message = 1;
}

message Response {
    string reply = 1;
}
